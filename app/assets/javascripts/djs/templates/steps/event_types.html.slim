div.page-block.registration
  .text-center
    img.small-logo.display-inline-block src="/images/icons/img-logo-zu-purple.png"
  hr
  .navigation-bar.text-center
    .step.small-text
      div.pull-left.slider-btn style="padding-right: 70px"
      | Step 1 of 4
      .pull-right
        a.small-text.slider-btn.next ng-click="next()"
          | Next
  hr
  div
    .question.text-center
      | What type of events do you want to play?
    .select.text-center.small-text
      | Select all that apply
    .container
      .error ng-repeat="error in validation_errors.event_categories"
        | {{ error }}
      label.select-button ng-repeat="event_type in event_types"
        .btn-checkbox.text-center class="{{ event_type.selected? 'active': '' }}"
          | {{ event_type.title }}
        input.hide type="checkbox" value="{{ event_type.id }}" ng-model="event_type.selected"
  div.tmp