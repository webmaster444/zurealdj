.wrapper.wrapper-content
  .row.animated.fadeInRight
    .col-md-12
      .ibox.float-e-margins
        .ibox-title
          h2
            | {{ genre.title }}
        .ibox-content

          .form-group
            .row
              .col-lg-2
                label.control-label
                  | {{ I18n.t('genre.fields.title') }}:
              .col-lg-10
                p.form-control-static
                  | {{ genre.title }}
            .hr-line-dashed

          .form-group
            .row
              .col-lg-2
                label.control-label
                  | {{ I18n.t('genre.fields.created_at') }}:
              .col-lg-10
                p.form-control-static
                  | {{ genre.created_at }}
            .hr-line-dashed

          .form-group
            .row
              .col-lg-2
                label.control-label
                  | {{ I18n.t('genre.fields.updated_at') }}:
              .col-lg-10
                p.form-control-static
                  | {{ genre.updated_at }}
            .hr-line-dashed

